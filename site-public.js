const search=document.getElementById('catalogSearch');const tabs=[...document.querySelectorAll('.tab')];const cards=[...document.querySelectorAll('.product-card')];let active='Todos';function filter(){const q=(search?.value||'').toLowerCase().trim();cards.forEach(card=>{const okText=(card.dataset.search||'').includes(q);const okCat=active==='Todos'||card.dataset.category===active;card.style.display=okText&&okCat?'':'none'})}search?.addEventListener('input',filter);tabs.forEach(btn=>btn.addEventListener('click',()=>{tabs.forEach(b=>b.classList.remove('active'));btn.classList.add('active');active=btn.dataset.category;filter()}));
const modal=document.getElementById('productModal');const img=document.getElementById('sheetImage');const kicker=document.getElementById('sheetPresentation');const title=document.getElementById('sheetTitle');const price=document.getElementById('sheetPrice');function openSheet(card){const pimg=card.querySelector('.product-visual img');img.src=pimg.src;img.alt=pimg.alt;kicker.textContent=`${(card.dataset.category||'').toUpperCase()} · ${card.dataset.sku||''}`;title.textContent=`${card.querySelector('.product-name').textContent.trim()} · ${card.querySelector('.product-kicker').textContent.trim()}`;price.textContent=card.querySelector('.price-box strong').textContent.trim();modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closeSheet(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}document.querySelectorAll('.card-btn').forEach(btn=>btn.addEventListener('click',()=>openSheet(btn.closest('.product-card'))));document.getElementById('sheetCloseX')?.addEventListener('click',closeSheet);document.getElementById('sheetCloseBtn')?.addEventListener('click',closeSheet);modal?.addEventListener('click',e=>{if(e.target===modal)closeSheet()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal?.classList.contains('open'))closeSheet()});
